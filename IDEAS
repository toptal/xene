_chat_ with _user|users_ at _channel_

bob = avenger.user({name: 'bob'}) // id or partial user object

chat = avenger.chat('avengers-general' /* userId | channel name */, 'direct' /* 'direct' | 'channel' | 'group' */)

chat.startThread('topic name', {qa: bob.id, lead: eve.id})

`hi {users.qa.name} and {users.lead.name}`
