language: node_js
node_js:
  - 8.3.0

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.27.5
  - export PATH="$HOME/.yarn/bin:$PATH"

env:
  - DIR=packages/core
  - DIR=packages/slack
  - DIR=packages/test

script: cd $DIR && yarn && yarn test
